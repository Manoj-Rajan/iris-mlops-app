name: Test CI Trigger

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Print success message
        run: echo "🎉 CI is working!"